<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="item_row_root_background">
        <item name="android:orientation">vertical</item>
        <item name="android:layout_marginBottom">@dimen/base_separation</item>
        <item name="android:layout_marginLeft">@dimen/base_separation</item>
        <item name="android:layout_marginRight">@dimen/base_separation</item>
        <item name="android:background">@drawable/card_ui_background</item>
    </style>

</resources>
